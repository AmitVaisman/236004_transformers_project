from setuptools import setup

setup(name='know-subnet',
      packages=['know_subnet'],
      version='0.1',
      description='A package to learn subnetwork masks over pretrained language models for relational knowledge.',
      install_requires=[])
